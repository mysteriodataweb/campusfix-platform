# App
PORT=5000
FRONTEND_URL=http://localhost:3000
FRONTEND_PUBLIC_URL=http://localhost:3000

# Database (Option 1: explicit vars)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=campusfixit

# Database (Option 2: single URL)
# DATABASE_URL=mysql://user:password@host:3306/campusfixit

# Railway MySQL variables can also be used directly:
# MYSQLHOST, MYSQLPORT, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=fixit3128@gmail.com
SMTP_PASS= 
SMTP_SECURE=false
FROM_EMAIL=no-reply@campusfix.dev

# Notification recipients
MANAGER_EMAIL=fredbiam9@gmail.com
TECHNICIAN_EMAIL=technician@campus.com
